import {HTMLAttributes} from "react";

export const DeDustIcon = (props: HTMLAttributes<HTMLImageElement>) => {
    return (
        <img
            {...props}
            alt="de dust icon"
            src='/icons/dedust.png'
        />
    )
}